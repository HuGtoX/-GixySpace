{
	"importMap": "./import_map.json", // 指定 Import Map 文件
	"lint": {
		"include": ["src/**/*.mts", "test/**/*.ts"], // 包含的文件
		"rules": {
			"tags": ["recommended"],
			"exclude": ["no-explicit-any"],
			"include": ["ban-untagged-todo"] // 保留其他规则
		}
	},
	"compilerOptions": {
		"strict": false
	}
}
